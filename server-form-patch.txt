// Adição para o formulário de criação - após apiToken
              <FormField
                control={form.control}
                name="maxUsers"
                render={({ field }) => (
                  <FormItem>
                    <FormLabel className="font-bold text-primary">Quantidade Máxima de Usuários</FormLabel>
                    <FormControl>
                      <Input 
                        type="number" 
                        min="1" 
                        placeholder="Ex: 10" 
                        {...field} 
                      />
                    </FormControl>
                    <FormMessage />
                    <FormDescription>
                      Quantidade máxima de usuários que podem se conectar a este servidor simultaneamente.
                    </FormDescription>
                  </FormItem>
                )}
              />

// Adição para o formulário de edição - após apiToken
              <FormField
                control={form.control}
                name="maxUsers"
                render={({ field }) => (
                  <FormItem>
                    <FormLabel className="font-bold text-primary">Quantidade Máxima de Usuários</FormLabel>
                    <FormControl>
                      <Input 
                        type="number" 
                        min="1" 
                        placeholder="Ex: 10" 
                        {...field} 
                      />
                    </FormControl>
                    <FormMessage />
                    <FormDescription>
                      Quantidade máxima de usuários que podem se conectar a este servidor simultaneamente.
                    </FormDescription>
                  </FormItem>
                )}
              />