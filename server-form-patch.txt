// Campo maxUsers para o formulário de criação - inserir após o campo apiToken
              <FormField
                control={form.control}
                name="maxUsers"
                render={({ field }) => (
                  <FormItem>
                    <FormLabel className="font-bold text-primary">Quantidade Máxima de Usuários</FormLabel>
                    <FormControl>
                      <Input 
                        type="number" 
                        min="1" 
                        placeholder="Ex: 10" 
                        {...field} 
                      />
                    </FormControl>
                    <FormMessage />
                    <FormDescription>
                      Quantidade máxima de usuários que podem se conectar a este servidor simultaneamente.
                    </FormDescription>
                  </FormItem>
                )}
              />
              
// Campo maxUsers para o formulário de edição - inserir após o campo apiToken no segundo formulário
              <FormField
                control={form.control}
                name="maxUsers"
                render={({ field }) => (
                  <FormItem>
                    <FormLabel className="font-bold text-primary">Quantidade Máxima de Usuários</FormLabel>
                    <FormControl>
                      <Input 
                        type="number" 
                        min="1" 
                        placeholder="Ex: 10" 
                        {...field} 
                      />
                    </FormControl>
                    <FormMessage />
                    <FormDescription>
                      Quantidade máxima de usuários que podem se conectar a este servidor simultaneamente.
                    </FormDescription>
                  </FormItem>
                )}
              />